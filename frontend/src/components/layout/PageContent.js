import React from 'react';

const PageContent = ({ children }) => (
  <div className="font-sans bg-gray-100 text-gray-900 overflow-hidden">
    {children}
  </div>
);

export default PageContent;
